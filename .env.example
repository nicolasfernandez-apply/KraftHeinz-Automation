# ── PRV Authentication (mandatory) ───────────────────────────────────────────
# The PRV environment is protected by Google Cloud IAP.
# Both fields are required — the test will fail immediately if either is missing.
PREVIEW_USERNAME=your-email@example.com
PREVIEW_PASSWORD=your-password
